overwrite: true
schema: ${REACT_APP_GRAPHQL_URL}
documents: "src/**/*.{ts,tsx}"
generates:
  src/generated/types.ts:
    config:
      noNamespaces: true
    plugins:
      - "typescript-common"
      - "typescript-client"
