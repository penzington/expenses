overwrite: true
schema: ${REACT_APP_API_URL}/graphql
documents: "src/**/*.{ts,tsx}"
generates:
  src/generated/types.ts:
    config:
      noNamespaces: true
    plugins:
      - "typescript-common"
      - "typescript-client"
